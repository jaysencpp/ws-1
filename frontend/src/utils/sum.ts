//TODO: Find usages/references of this function
//TODO: Add return type to function, but don't write the return type yourself, use refactoring tool
export function sum(a: number, b: number) {
  return a + b;
}
